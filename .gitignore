# Secrets
.env
.env.local
.env.local.web
web/.env.local

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Node / Next
node_modules/
.next/
out/
*.tsbuildinfo

# Volatile scan state, rewritten on every run.
# The catalogs, the analysis cache and the packaged snapshot stay tracked —
# the cache in particular is expensive inference we do not want to re-pay.
data/jobs_store.json
data/scan_status.json

# OS
.DS_Store
